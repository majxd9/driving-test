name: Practical Info Build Check

on:
  push:
    branches:
      - practical-info-overhaul-v2
      - practical-info-overhaul-v3
      - practical-scenes-rebuild-v4
  pull_request:
    branches:
      - main

jobs:
  build-client:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: client
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: client/package-lock.json
      - name: Install
        run: npm ci
      - name: TypeScript + Vite build
        run: npm run build
