import {useEffect,useState} from 'react';
type Props={src:string;alt:string;className?:string;priority?:boolean;sizes?:string;objectFit?:'contain'|'cover'};
const V='2026-09-11'; const ver=(u:string)=>u?.startsWith('/')?`${u}${u.includes('?')?'&':'?'}v=${V}`:u;
export default function OptimizedImage({src,alt,className='',priority=false,sizes,objectFit='contain'}:Props){const[loaded,setLoaded]=useState(false);const[failed,setFailed]=useState(false);useEffect(()=>{setLoaded(false);setFailed(false)},[src]);if(!src)return null;return <div className={`relative overflow-hidden ${className}`}>{!loaded&&!failed&&<div className="absolute inset-0 skeleton"/>}<img src={ver(src)} alt={alt} sizes={sizes} loading={priority?'eager':'lazy'} decoding="async" fetchPriority={priority?'high':'auto'} onLoad={()=>setLoaded(true)} onError={()=>{setFailed(true);setLoaded(true)}} className={`block w-full h-full object-${objectFit} transition-opacity duration-200 ${loaded?'opacity-100':'opacity-0'}`}/>{failed&&<div className="absolute inset-0 grid place-items-center text-xs text-muted bg-paper px-4 text-center">الصورة غير متاحة لهذا السؤال.</div>}</div>}
